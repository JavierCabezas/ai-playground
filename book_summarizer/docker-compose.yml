version: "3.9"
services:
  app:
    build: .
    volumes:
      - ./models:/app/models
      - ./data:/app/data
      - hf_cache:/root/.cache/huggingface
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  hf_cache: